# PartyFinder frontend

De webclient staat in [`raidless/`](raidless/README.md). Het is een React- en TypeScript-app die de PartyFinder API, IdentityServer, SignalR en Stripe gebruikt.

```powershell
cd .\raidless
npm install
Copy-Item .env.example .env.local
npm run dev
```
